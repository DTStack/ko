const prettier = require('./prettier');
const eslint = require('./eslint');
const stylelint = require('./stylelint');
const mdlint = require('./mdlint');

module.exports = {
  prettier,
  eslint,
  stylelint,
  mdlint,
};
